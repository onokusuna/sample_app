<h1>投稿一覧</h1>
	<!-- eachでループ処理（投稿データの数だけ繰り返す） -->
	<% @lists.each do |list| %>
		<p>タイトル</p>
		<span>
			<%= link_to link_to list.title, todolist_path(list.id) %>
		</span>
		<!-- 8章で追記 画像表示を追加 Refile特有の書き方 -->
		<p>画像</p>
		<%= attachment_image_tag list, :image, :fill, 100, 100, format: 'jpeg' %>
	<% end %>