<h1>編集画面</h1>
<%= form_for(@list, url: "/todolists/#{@list.id}") do |f| %> <!-- 最後にエンド -->
	<h4>タイトル</h4>
	<%= f.text_field :title %>
	<h4>本文</h4>
	<%= f.text_area :body %> <!-- .erb -->
	<%= f.submit '保存' %> <!-- .erb -->
<% end %>  <!-- .erb -->